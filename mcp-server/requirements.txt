# Pinned: the handler/transport-security wiring is validated against this exact
# version (FastMCP internals like lifespan + DNS-rebinding defaults vary by release).
mcp==1.28.0
httpx>=0.27.0
mangum>=0.19.0
PyJWT[crypto]>=2.10.0
# Local HTTP transport only (not used on Lambda):
uvicorn>=0.30.0
