# Dependencies
node_modules/

# Python
__pycache__/
*.py[cod]

# Build output
dist/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local

# AWS SAM
.aws-sam/
# Note: samconfig.toml IS committed — CI (sam deploy) needs stack_name/region
# from it. Keep secrets out of it (we use resolve_s3 + OIDC, so it has none).

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
